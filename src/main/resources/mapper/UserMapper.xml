<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.dal.mapper.UserMapper">
  <resultMap id="BaseResultMap" type="com.boot.dal.dao.User">
    <!--@mbg.generated-->
    <!--@Table `user`-->
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="is_deleted" jdbcType="BOOLEAN" property="isDeleted" />
    <result column="create_time" jdbcType="BIGINT" property="createTime" />
    <result column="update_time" jdbcType="BIGINT" property="updateTime" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="phone" jdbcType="VARCHAR" property="phone" />
    <result column="avatar" typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler" property="avatar" />
    <result column="sex" jdbcType="VARCHAR" property="sex" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    id, is_deleted, create_time, update_time, username, `password`, phone, avatar,
    sex
  </sql>

  <resultMap id="data" type="com.boot.dto.vo.UserDataVo">
    <association property="userInfo" javaType="com.boot.dto.common.vo.UserBasicInformation">
      <id column="id" property="id"/>
      <result column="username" property="username"/>
      <result column="avatar" property="avatar"/>
      <result column="sex" property="sex"/>
      <result column="mark" property="mark"/>
    </association>
  </resultMap>

  <select id="getData" resultMap="data">
    select id,username,avatar,sex,mark,
      (select count(*) from collect where is_deleted = 0 and user_id = u.id) as collect,
      (select count(*) from dynamic where is_deleted = 0 and user_id = u.id) as dynamic,
      (select count(*) from goods where is_deleted = 0 and user_id = u.id) as publish,
      (select count(*) from `order` where is_deleted = 0 and goods_user_id = u.id and status = 'FINISHED') as sold,
           (select count(*) from `order` where is_deleted = 0 and user_id = u.id and status = 'FINISHED') as sold
    from user as u where is_deleted = 0 and id = #{userId,jdbcType=VARCHAR}
  </select>
</mapper>